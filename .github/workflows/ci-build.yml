name: ci build docker image and push to dockerhub

on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"

jobs:
  build:
    uses: ./.github/workflows/lib-build-and-push-with-ver.yml
    with:
      version: ${{ github.ref_name }}
    secrets: inherit
